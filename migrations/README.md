# Migrations

SQL migrations for ProvenanceX SQLite/PostgreSQL build telemetry and evidence history database:
* `001_initial_schema.sql`: Tables for projects, repositories, builds, evidence items, and correlation decisions.
